# Grafana for Your Data

An MVP of an application that collects your data and allows you to calculate the interdependencies between them; at the moment, it is only possible to calculate correlations.

To run program exec:

```
cargo run
```